package backend.modding.songs;

/**
 * A scripted song event module. Used, so scripts can extend them.
 */
@:hscriptClass
class ScriptedSongEventModule extends SongEventModule implements polymod.hscript.HScriptedClass {}
